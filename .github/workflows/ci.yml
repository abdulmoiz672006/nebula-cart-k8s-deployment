- name: Create Kind Cluster
      uses: helm/kind-action@v1.10.0
      with:
        cluster_name: nebula-test-cluster

    - name: Kubernetes Manifests Validation
      run: |
        # Ab cluster mojood hai, toh validation sahi se hogi
        kubectl apply -f k8s/
        kubectl get all

