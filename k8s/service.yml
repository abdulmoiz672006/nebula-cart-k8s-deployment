apiVersion: v1
kind: Service
metadata:
  name: online-shop-service
  namespace: online-store
spec:
  selector:
    app: online-shop
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173

